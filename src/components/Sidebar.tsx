import {ListItems} from "./ListItems";

export const Sidebar = () => {
    return (
        <aside className="p-3 h-full w-[25%] bg-white flex items-center justify-center">
            <ListItems />
        </aside>
    );
};
